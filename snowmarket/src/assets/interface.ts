export interface users {
  id: string;
  username: string;
  email: string;
  birthday: string;
  points: number;
}
export interface products {
  id: number;
  name: string;
  stock: number;
  price: string;
}
